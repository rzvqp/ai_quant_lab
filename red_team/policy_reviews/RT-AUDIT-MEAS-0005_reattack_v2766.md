# RED TEAM — RE-ATTACK · canonical_evaluator v2.7.66 @ `3344bff` (the extended-18 gate)
### RT-AUDIT-MEAS-0005 · the four corrections (MEAS-9 asymmetric, MEAS-10 concentration, NewType units, run_hash)
**Date:** 2026-08-13 · **Auditor:** Red Team · **Mandate:** CEO — re-attack `canonical_evaluator` @ `3344bff` (diff off `2403aad`); verify the four v2.7.66 corrections; checks (a) Test 18 both branches + INVALID counted **in the report**, (b) MEAS-10 in report not just tests, (c) NewTypes unwritable or merely improbable + bypass hunt, (d) `compare()` on all paths or bypassable, (e) T12/13 resolved by types or moved, (f) **a tenth divergence**; verify the **S3 ~51% INVALID_EXECUTION** figure (real or asymmetric-implementation artifact) and the **S3 saga** (+0.23 → +0.395 → −0.13 → −0.17). **No engine modified; no repair; no real data.** Verified on imported source @3344bff + synthetic fixtures + VE's own suites re-run (23 passed, 2 skipped).

## VERDICT — **PASS_WITH_LIMITATIONS.** The measurement semantics pass the extended 18; the *comparability/provenance* guards are not yet structural.
All four corrections are **real and correctly implemented at runtime.** MEAS-9 (asymmetric gap-guard), MEAS-10 (concentration in the official report), and T12/13 (spread full/half via `half_of`) are **genuinely closed** — this is the first evaluator whose per-trade measurement I cannot break on the normal or gap paths. **But four limitations block a clean full-contract ratification:** the NewType guarantee is runtime-erased **and un-gated** (no mypy anywhere in either repo); `compare()` is never wired into an aggregation path (opt-in, bypassable); **MEAS-14 (the tenth) persists** (rejection is scenario-invariant); and a **new eleventh** — `run_hash` omits `block_end`, so two runs on different block cuts are falsely comparable. The S3 51% is **plausibly real, not an artifact**, but is **not reproducible from committed code**.

---

## THE FOUR CORRECTIONS — verified CLOSED
- **(a) MEAS-9 asymmetric — CLOSED, both branches, and INVALID is COUNTED in the report.**
  - §5(a) `directional_risk = dirn·(entry−stop) ≤ 0` → `InvalidExecution` (verified: long entry 97 / stop 98 → `InvalidExecution`, `directional_risk=−1.0`, **not** the old +0.95 win). It is **counted and reported** (`StrategyReport.invalid_executions`, verified =1) and **excluded** from `eligible`/returns. The silent `continue` is gone — INVALID does not vanish. ✅
  - §5(b) `reward = dirn·(target−entry) ≤ 0` → exit at **entry price**, `exit_reason="gap_through_target"`, `R = (0 − cost)/risk` (verified: entry 105 / target 102 → exit@105, base_R = −0.007, **not** the old forced −0.436). ✅
  - The asymmetry is principled (fraction structure: (a) destroys the denominator → undefined → excluded; (b) zeros only the numerator → defined → −cost). Correct.
- **(b) MEAS-10 — CLOSED in the official report.** `StrategyReport` now carries `base_concentration`/`stress_concentration` (`Concentration`): `best_trade_share` as a `LevelOutput` (**`Unavailable("net_non_positive")` when sum_R ≤ 0** — verified), plus `trimmed_top1_avg_R`, `n_trimmed`, `trimmed_fraction`, `sum_R`, `wo1_still_positive`. Trim = ceil-min-1, order (R DESC, entry ASC). The CEO fat-tail guard (R10) is now computable from official output, not just tests. **Independently confirmed.** ✅
- **(e) T12/13 — resolved by ARITHMETIC (the types only guard it).** R3 = `minimum_stop_distance(half_of(spread_full), atr)`; `K_SPREAD(2) × half = 2×(full/2) = full`. Verified: BASE R3 term = 0.05, STRESS = 0.08 (= full spread **once**); R4 cost = full spread **once** (0.05 / 0.24). The double-count is gone; the "2×" cancels against the "/2". **The fix is `half_of`, not the NewTypes** — the types make a forgotten `half_of` mypy-catchable, they do not perform the resolution. ✅ (moved? **no — resolved**, subject to (c).)
- **run_hash — present and raising.** `run_hash = sha256(config_hash ‖ sha256(data_identity))`; `RunContext` carries symbol/timeframe/split/manifest/n_blocks/holdout; `compare()` raises `NonComparableError`. Two different-data runs get different hashes (verified). ✅ (subject to (d) and the eleventh.)

## LIMITATIONS (block full-contract ratification, not the per-trade semantics)
- **(c) NewTypes are runtime-erased → UNWRITABLE is overclaimed; and there is NO mypy gate.** `NewType` is a static-only device: at runtime `minimum_stop_distance(SpreadFull(0.05), atr)` returns **0.10 (the DOUBLE)** with **no error** — I wrote the "unwritable" unit error in one line. It is caught **only** by `mypy --strict`, and **neither `ai_quant_lab` nor `ai_quant_lab-alpha-automation` contains any mypy config, pre-commit hook, or CI workflow** (git-verified). So the guarantee is currently enforced by **nothing** in the pipeline. Bypasses: direct call with a raw/`SpreadFull` float, `cast`, `Any`, `# type: ignore`. **Verdict: mypy-improbable *if* strict mypy is gated — but it is not gated, so today it is unenforced.** Fix: gate `mypy --strict` in CI, **or** make `half_of` the only constructor and runtime-check the half at the R3 boundary.
- **(d) `compare()` is never invoked internally → procedural, bypassable.** `evaluate_signal`/`evaluate_strategy` **produce** `run_hash` but **never call** `compare()`/`require_comparable()` (verified: zero internal call sites beyond the defs). Nothing stops `repA.base_mean_R − repB.base_mean_R` across mismatched runs; the guard fires **only if a consumer remembers to call it.** This is the **same procedural-not-structural pattern** I flagged at E2E-L2 (`build_market_state` guards bypassable by direct construction). Fix: invoke `require_comparable()` at every aggregation/leaderboard boundary, or make comparison only reachable through a guarded combinator.
- **(f) MEAS-14 (the TENTH) persists.** R3 rejection is decided **once**, from `sig.spread_price`, **before** the scenario loop → BASE and STRESS share **one** rejection population. The CEO's per-scenario R3 components (BASE 0.05 / STRESS 0.08) require **different** floors → **different** populations, which the architecture still cannot express. Unchanged from RT-AUDIT-MEAS-0004. To honour per-scenario costs, the floor and rejection must be evaluated per scenario.
- **(ELEVENTH, new) `run_hash` omits `block_end`.** `block_end` is a per-**call** argument, absent from `run_hash`; nothing binds it to `block_manifest_hash`/`n_blocks`. Verified: same `RunContext`, `block_end=5` vs `18` → **identical run_hash** but different results (`still_open_at_end` vs `time`). Two runs on **mismatched block boundaries are falsely comparable** — `compare()` would not raise. `run_hash` covers the **declared** data identity, not the **actual** cut used. (T17-completeness residual; deeper than the earlier data-blind finding.)

## THE S3 ~51% INVALID_EXECUTION — plausibly REAL, not an asymmetric-implementation artifact (but not reproducible from committed code)
§5(a) fires **iff** `dirn·(entry−stop) ≤ 0` — exactly "the entry is on the wrong side of its own stop", **exact for both directions**; it **cannot** false-positive a correctly-oriented signal. So a 51% rate is **upstream** (a property of S3, or an S3→`Signal` **adapter** orientation bug), **never** produced by the evaluator's asymmetry itself. Mechanism probe (synthetic breakout, stop **at** the broken level, entry = next open oscillating around it): INVALID lands in the **tens of percent by construction** — when the stop sits at the level the entry oscillates around, ~half the next-opens fall on the wrong side. **So 51% is structurally plausible and, if real, is the audit's most important finding: ~half of S3's population was never executable as specified** (the old engines were booking much of it — some as the +0.95 fictional wins I found).
**Caveat (blocking reproducibility):** **no committed code computes this figure** — the S3→`Signal` adapter and the run are not in either repo (VE ran it ad-hoc). I cannot independently confirm 51% vs an adapter orientation bug without it. **Recommend: commit the adapter + the run under a `run_hash`** so the 51% is reproducible and the orientation is auditable.

## THE S3 SAGA — internally consistent; −0.17 is the most-correct number **so far**, no new error found (conditional on 51% being real)
`+0.23` (tick-corrected cost) → `+0.395` (cost repaired 0.05 **but** fictional gap-open wins) → `−0.13` (gap symmetric) → `−0.17` (gap asymmetric, 51% INVALID). **The −0.17 < −0.13 direction is EXPECTED, not a new error:** under symmetric treatment the stop-gaps exited at entry (≈ −cost ≈ −0.01, **above** the −0.13 mean); asymmetric **removes** those 51% entirely → the surviving worse trades average lower (−0.17). Arithmetically consistent. **−0.17 is honest for the executable 49% under BASE**, but: (1) **non-comparable** to +0.23/+0.395/−0.13 — each is a different population/config (R11; the new `run_hash` enforces this **iff** `compare()` is called — see (d)); (2) it is **BASE/uncalibrated** — must be paired with STRESS; (3) it is conditioned on executability — a **strong selection effect** (half the signals dropped) that must be reported *with* the number. **No residual error in −0.17 itself**, provided the 51% is real (§ above).

## TWO CEO SIDE-ITEMS
- **Cost-vs-population decomposition (spec §4) — NOT required for the gate.** The conformance gate tests per-trade correctness; the decomposition attributes the S3 saga's movement to cost vs population. It is needed to **interpret** the saga honestly (so a population-driven change is not mis-read as a cost effect — the manifest itself flags it as path-dependent), **not** to ratify the evaluator. Recommend it as an S3-reporting requirement, not a gate precondition.
- **T4 non-gap (entry-bar target precedence, no gap) — correctly still BLOCKED.** VE's conformance file marks it `@pytest.mark.skip("BLOCAT: sub-spec Statistician")`. The code implements count-with-SL-first, but the precedence is not ratified. Left at the Statistician, as directed. ✅

## VERDICT — **PASS_WITH_LIMITATIONS.**
**Measurement semantics: PASS.** MEAS-9 (both branches, INVALID counted/reported/excluded, never a win), MEAS-10 (concentration in the official report), and T12/13 (spread full/half via `half_of`) are correctly closed; VE's suite is genuinely at **18** (gap-open both branches; T4 non-gap correctly blocked). **Comparability/provenance enforcement: NOT yet structural** — types unenforced (no mypy gate), `compare()` un-wired (bypassable), MEAS-14 (tenth) persists, and the eleventh (`run_hash` omits `block_end`) makes mismatched-cut runs falsely comparable. **Recommendation to CEO: the contract may be ratified for the per-trade measurement semantics now; withhold ratification of R11's comparability clause until (c)(d) and the eleventh are structural and MEAS-14 is resolved or explicitly accepted.** The S3 51%, if reproducible from a committed adapter, is the audit's most consequential result: half of S3 was never tradeable.

## HANDOFF → CEO / Statistician
1. **Close before R11 ratification:** gate `mypy --strict` in CI (or runtime-check the half at R3); wire `require_comparable()` into every aggregation/leaderboard path; add `block_end`/actual-cut to `run_hash`; resolve MEAS-14 (per-scenario floor+rejection) or record it as accepted.
2. **Commit the S3→`Signal` adapter + the 51% run** under a `run_hash` so the figure is reproducible and the orientation auditable; then I re-verify 51% real-vs-artifact from source.
3. **S3 numbers:** publish −0.17 with STRESS, mark BASE uncalibrated + the 51%-excluded selection, and never compare across the saga (different run_hash).
4. **Decomposition §4:** require it for S3 reporting, not for the gate. **T4 non-gap:** stays at the Statistician.

Red Team designed no remedy, modified no engine, ran no data on the market, changed nothing outside `red_team/`.
