# RED TEAM — FINAL VERIFICATION · PASS_FOR_LIVE_SHADOW
### RT-MANDATE2-0003 · **PASS_FOR_LIVE_SHADOW · READY_FOR_CEO_LIVE_SHADOW_AUTHORIZATION**
**Date:** 2026-08-17 · **Auditor:** Red Team · **Target:** AI Trader `READY_FOR_LIVE_SHADOW_REVIEW_CANDIDATE_V2_CORRELATED` — code `6e5a333`, report `bf9243d`, branch `ai-trader-implementation` (local == `trader/ai-trader-implementation`). Tower artefact `ve_tower 0.5.2` (`1abcd60d…c28d8`, `TOWER_CHAIN_ATR_PASS`, RT-TOWER-0010 `68c6b59`). Supersedes the CONDITIONAL of RT-MANDATE2-0002. No engine modified; SYNTHETIC/fixture data + read-only real MT5 for broker evidence; nothing changed outside `red_team/`.

# VERDICT — **PASS_FOR_LIVE_SHADOW · READY_FOR_CEO_LIVE_SHADOW_AUTHORIZATION**
The single correlated chain that RT-MANDATE2-0002 required is now demonstrated: **one** `bridge.evaluate_bar` call carries **one** market event through the genuinely installed components — N1 → Router/Eligibility → IPC v3 → isolated worker → `run_tower_chain` → real N2/N3/N4 → `DecisionRequest` → cost model → EV → N6 → `TRADE` → Risk Manager (approved) → Execution Adapter → **broker gate BLOCKED** — with one shared, independently-recomputable identity spine and **no** hand-built terminal object, **no** direct N2/N3/N4 call, **no** bypass. All PASS conditions are met. **LIVE_SHADOW is NOT started; the CEO grants the single remaining approval separately.**

---

## §1 — Delivery · PASS
`6e5a333` (code, "ve_tower 0.5.2 + single correlated run") and `bf9243d` (report) exist; branch head `bf9243d == trader/ai-trader-implementation` (local==remote); `6e5a333` descends from the HOLD `ee92c8c`. Report ↔ code ↔ `CANDIDATE_V2_CORRELATED_EVIDENCE.json` consistent. Worker/artefact install: ve_tower 0.5.2 `pip install --no-deps` into the **tower venv only, non-editable** (`ve_tower.__file__` → site-packages); worker package reinstalled non-editable; **main venv confirmed clean** (`pip show ve-tower ve-tower-worker` → not found, before and after). **Pin: `verify_pin(real_handshake_identity) == ()`** — zero mismatches (15 exact-match fields + new `atr_source_commit`), against a genuine non-stub `EstablishedSession`. **Rollback proof:** installing 0.5.0 → real handshake **refused** (`HANDSHAKE_IDENTITY_MISMATCH: expected='0.5.2' actual='0.5.0'`). Sidecar `HANDOFF_MANIFEST-0.5.2.json` authoritative (SHA/build `b0cf2ea`/state `60bf71b`/`atr_source`).

## §2 — The single correlated path (DECISIVE) · PASS
`demonstrate_candidate_v2_correlated.py` runs **one** path, not three glued proofs. Live MT5 was honestly probed first (`mt5_live_probe_result`: `UNCERTAIN_REGIME`/`TRUE_RANGE_NOT_IDENTIFIABLE` on all four strategies — captured, not discarded), so the run falls back to the **versioned canonical fixture** `trend_up_regime_bars` (the CEO's explicitly-allowed second option), never fabricating a decision. Then a **single** `bridge_module.evaluate_bar(last_bar, timeframe="M15", axes_builder=builder, tower=tower)` produces `approved_outcome`; the **real** `approved_outcome.event_identity` and `approved_outcome.decision` are read back (never rebuilt) and the same outcome flows `submit_new_brain_candidate → attempt_shadow_execution → broker gate`. Verified in source (`bridge.evaluate_bar` @ `6e5a333`): N1 `observe` → `StrategyRouter.eligible` (ineligible → `decision=None`, no N6 call) → `resolve_cost_components` → `tower.client.request_chain` (IPC v3) → `side = _side_from_strategy(canon)` → real chain identities propagated into `EventIdentity` via `replace(...)` → `ve_brain.decide_n6` → provenance from the real response. **Independently ran the AST guard** (`test_correlated_evidence_ast_guard`'s own walk, string-constants included) over the script: **zero** hits for `EventIdentity`/`DecisionResponse`/`DecisionProvenance`/`N2/N3/N4Response`, `run_n2/n3/n4`, `set_authority`, `order_send`, or `bias_direction="LONG"`. Worker `decision.py` calls **exclusively `run_tower_chain`** (run_n2/n3/n4 never imported/referenced). `confirmation_available` comes from `chain_result` (real N4), not an override. **No manual construction, no direct API, no bypass.**

## §3 — Identity correlation · PASS (independently recomputed)
One common spine: `market_event_id="XAUUSD:M15:430200"`, `trace_id=44ab1b61…`, `configuration_fingerprint=3d8a8b6c…`, `worker_session_id=ed489567…`, `worker_identity_fingerprint=337660b5…`, `tower_version=0.5.2`, `chain_binding_version=tower-chain-binding-v1`, `chain_response_contract_version=tower-chain-response-v1`, `chain_fingerprint=112749852f…`, `chain_status=ok_chain`. Distinct, propagated per-node identities: N2/N3/N4 each carry their own `node_input_fingerprint` + `data_identity` (real `bars_content_hash`, per-timeframe H1/M15/M5, all `as_of=430200`), N2 `output_fingerprint=ff6266af…`, plus N3→N4 link, ATR provenance, `strategy_id=trend_pullback`, `cost_model_fingerprint=860e2088…`, N6 provenance (`ev-core@bdd15e5+ev-adapter-v1`). **Decisive recompute:** I independently computed `ve_tower.event_fingerprint("XAUUSD:M15:430200","XAUUSD",430200) = 997d40de3e8aa57f` — **exactly** the `event_fingerprint` shared by N2, N3, and N4 in the evidence. The correlated event is genuine and reproducible; N2/N3/N4 agree on it (single event), with legitimately distinct per-timeframe data identities.

## §4 — Strategy & probability · PASS (limitation declared, production fail-closed)
`side` = `StrategyContract.allowed_directions[0]` (`_side_from_strategy`) — **never** a default, operator input, hidden fixture, `bias_direction`, or an N2 copy (every canonical strategy is `allowed_directions=("LONG",)` → `side=1`, a genuine catalog fact). `probability_inputs` is the **one** declared value fixture, labeled `TEST_ONLY_CANONICAL_FIXTURE`, monkeypatched only at the evidence script's `bridge_module.load_probability_inputs` call site — never production, never claiming economic edge. **Production `load_probability_inputs` returns `None` unconditionally** (no ratified outcome-count table exists yet), so production **fail-closes** without ratified statistics. Per the CEO's own allowance, this declared limitation does not block LIVE_SHADOW. (Also disclosed, not a defect: `router_bias_direction` has no real upstream source yet — defaults `None`, never used to derive `side`.)

## §5 — Worker & IPC · PASS
Chain protocol v3 (`TowerChainRequest`/`TowerChainResponse`); worker runs **`run_tower_chain` exclusively**; `_ALLOWED_CHAIN_REQUEST_FIELDS` rejects unknown fields; caller cannot supply `n2_fingerprint`/`bias_available`/ATR (structural + `UNKNOWN_REQUEST_FIELD`, re-verified in RT-TOWER-0008/0010); HMAC session binding; artifact pin exact (15+1 fields); `PRODUCTION_ENTRYPOINT=run_tower_chain`, direct APIs `UNBOUND_DIRECT_API`; stale-session/wrong-0.5.0-worker/loopback all fail-closed (rollback handshake refusal proven in §1). Worker unavailable → NO_TRADE/TOWER_UNAVAILABLE.

## §6 — Cost model · PASS
`bridge.evaluate_bar` consumes exclusively `resolve_cost_components(tier="BASE")`; `BASE_RATIFIED(0.05, 0.00, 0.00)` and `STRESS_RATIFIED(0.08, 0.08, 0.08)` are distinct; fingerprint ≠ `cost_model_configuration_fingerprint()` → `COST_MODEL_FINGERPRINT_MISMATCH`, resolution failure → `COST_MODEL_UNAVAILABLE` (both degrade every strategy that bar); no hardcoded cost literals in `bridge.py` (uses `cost_components.*`); cost provenance recorded on the `CostModel` node trace (`cost_model_fingerprint=860e2088…`, extrapolation-outside-window disclosed). Economic ratification not reopened.

## §7 — Risk & broker · PASS
Same run: `decision=TRADE`, `approved_upstream=True`, `risk_approved=True`, `reached_broker_gate=True`, `broker_blocked=True`, `gate_enabled=False` (block reason: "order submission is DISABLED — default-closed"). `BrokerOrderSubmissionGate` is a no-setter dataclass, `enabled=False` default, only an explicit source-visible `enabled=True` could flip it. **Real read-only MT5 broker evidence:** positions/orders 0→0, balance 1800.34→1800.34, equity 1800.34→1800.34, `broker_state_unchanged=True`, `order_send_calls=0`, `orders_created=0`, `positions_created=0`. `order_send` never imported/called (AST-verified). The two operator-gated real-order tests were correctly **not executed**.

## §8 — Authority & legacy · PASS
`set_authority` is **never called** in production or the evidence script (docstrings only; AST guard confirms). Current authority unchanged (default LEGACY); atomic switch mechanism present but inactive. After a future NEW_BRAIN activation, legacy becomes telemetry-only and cannot reach Risk/Execution; new-brain unavailable → NO_TRADE, zero fallback to legacy/market_intelligence. **Not activated during this verification.**

## §9 — Regression · PASS (authentic)
Log `full_regression_output_rt_tower_0010.txt` tail: **`3407 passed, 2 skipped, 4 warnings in 21347.47s (5:55:47)`**, **`EXIT_CODE=0`**; targeted 310/310; mypy `--strict` clean. The 2 skips are operator-gated real-terminal/real-order tests; the 4 warnings are the pre-existing E000 divide-by-zero in `vendor/alpha_automation_detectors/market_state.py:92` (unrelated to Mandate-2). **Full 6-hour re-run not repeated** (CEO §9 allowance): the commit is exact, the log/exit are authentic, and the decisive delta + path were **re-executed independently** — the tower chain (RT-TOWER-0010: 76 tests + full `ok_chain` path), the shared `event_fingerprint` recompute, the AST guard, and full production-path source inspection.

---

## VERDICT & NEXT STEP
**PASS_FOR_LIVE_SHADOW · READY_FOR_CEO_LIVE_SHADOW_AUTHORIZATION.** Every PASS condition holds: unique correlated path; no hand-built terminal object; N2/N3/N4 + ATR from the real 0.5.2 artefact; Router/Eligibility/EV/N6 not bypassed; identities propagated and independently verified; worker isolated and pinned; cost model correct; Risk approves; broker gate reached and blocks; zero orders/positions; regression authentic; authority not activated; legacy cannot take over after the future switch.

**Red Team does NOT start LIVE_SHADOW.** The CEO issues the single remaining approval separately. Even after LIVE_SHADOW starts, **the broker stays DISABLED**. **Alpha stays `ALPHA_BLOCKED_CANONICAL_N1_HANDOFF`; CAND-T05 frozen.** Red Team modified no engine, ran no real orders, changed nothing outside `red_team/`.
