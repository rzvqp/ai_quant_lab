# RED TEAM — N1 CANONICAL REPLAY HANDOFF · final revalidation
### RT-N1-0001 · **N1_HANDOFF_PASS · ALPHA_CANONICAL_RERUN_AUTHORIZED**
**Date:** 2026-08-17 · **Auditor:** Red Team · **Target:** `ve_n1_replay 0.1.0`, wheel `ve_n1_replay-0.1.0-py3-none-any.whl` SHA-256 `372b35f9…0eb3f1` (61,594 bytes), build `1f9e746`, delivery/state `8e6eef2`, wheel-commit `1379432`, sidecar `HANDOFF_MANIFEST-0.1.0.json`. AI handoff `21ae632`, detectors `61cbd58c`, ve_brain `0.1.3`. **LIVE_SHADOW remained active and untouched (read-only checks); broker DISABLED; Alpha has not run the 355 hypotheses.** No engine modified; SYNTHETIC data + git reanchoring; nothing changed outside `red_team/`.

# VERDICT — **N1_HANDOFF_PASS · ALPHA_CANONICAL_RERUN_AUTHORIZED**
The standalone N1 replay artefact is a byte-identical, independent closure of AI Trader @`21ae632` + detectors @`61cbd58c`, consuming pinned `ve_brain 0.1.3`. Every material condition passes: exact identity, git-verified byte-integrity (including the critical `market_structure = 52bb1eba`, **not** ve_tower's `d734ac9a`), fail-closed transactional bootstrap, wheel-vs-source parity, content-bound data identity, deterministic causal replay, complete independence, 18/18 tests + mypy clean. This PASS authorizes installing the exact wheel **only in the Alpha environment** and rerunning the 355 hypotheses. It does **not** authorize the broker and does **not** modify LIVE_SHADOW.

---

## §1 — Artefact identity · PASS
Wheel SHA-256 `372b35f9…0eb3f1` and size **61,594 bytes** exact; **git-stored bytes == working wheel**; build `1f9e746`, wheel-commit `1379432`, delivery/state HEAD `8e6eef2`. Sidecar describes the wheel exactly: package `0.1.0`, `py>=3.12`, `numpy>=1.24`, **ve_brain 0.1.3 wheel SHA `edd208ad…987d11` (independently verified against the pinned wheel — exact)**, `router-v1`, `detector_configuration_fingerprint=effa0663b8a45bb5`, contracts `n1-replay-request-v1` / snapshot `n1-replay-snapshot-v1` / reason-codes `n1-replay-reason-codes-v1`. **`vendored_source_identity` independently recomputed** via the documented algorithm (`"ai <relpath> <blob>"`@21ae632 / `"det <name> <blob>"`@61cbd58c; sort; join `\n`+trailing; sha256) → `sha256:1d4f6c48…06a190` == manifest.

## §2 — Closure & byte-integrity · PASS
Reanchored from git: **15 AI modules @`21ae632`** (`git rev-parse 21ae632:ai_trader/<path>` == wheel `git hash-object` == manifest, 15/15) and **5 detectors @`61cbd58c`** (`git rev-parse 61cbd58c:code/<det>` == wheel == manifest, 5/5). **CRITICAL POINT CONFIRMED:** `market_structure.py` in the wheel is git-blob **`52bb1eba`** (the @61cbd58c version), **NOT** ve_tower's `d734ac9a` — verified distinct. The closure is real: `market_structure`/`market_state`/`imbalance_mechanics`/`order_flow`/`order_block_void` + the ai_trader tail through `market_scanner.exceptions`, all byte-identical. No silent substitution.

## §3 — Bootstrap & collisions · PASS
**Precise characterization:** the loader registers the vendored modules **in `sys.modules` under their real names** (`ai_trader.*` namespace packages + leaf modules; detectors under bare names), each stamped `__ve_n1_replay_vendored__`, loaded from the wheel's `_ai`/`_det`. So it is **not** "zero `ai_trader` in `sys.modules`" — it is "the `ai_trader.*` names hold the vendored byte-identical modules, with no dependency on an installed ai_trader repo; a **foreign** occupant fails closed." Attacks (all from the installed wheel, 18/18): collision at **first / middle / last** detector, foreign `ai_trader` package, foreign `ai_trader.n1_replay`, foreign mid-order `ai_trader` module, **foreign ve_tower `market_structure`** → each raises `N1ReplayLoadCollisionError` with **zero leftover vendored modules, host object identity preserved, `_loaded` stays False, no stray modules**; `exec_module` exception → **original exception preserved**, full rollback; **clean retry succeeds** after removing the occupant; 8 concurrent imports thread-safe; two `N1ReplayEngine` instances have distinct identities and no shared state.

## §4 — Parity (wheel vs source) · PASS
**Byte-identity is the parity proof:** the wheel's 15 AI modules == source @`21ae632` and 5 detectors == @`61cbd58c` (§2, git-verified content hashes), so the wheel IS the source, byte-for-byte. Additionally, `new_brain_bridge/raw_axes_builder.py` is byte-identical blob **`d071c8cb` at both `21ae632` (wheel source) and `eb97a80` (the LIVE_SHADOW runtime that journaled the real bars)** — the wheel's N1 IS the runtime's N1. Behavioral confirmation from the installed wheel in the empty venv: the canonical **TREND_UP fixture (478 bars)** → `availability_status=FULL`, `applicable_regimes={TREND_UP}`, deterministic across runs and two instances; an axis-flipping OHLC change flips the regime `TREND_UP → TREND_DOWN` and the `output_fingerprint`. **The 16 real bars** journaled by LIVE_SHADOW all resolved `UNCERTAIN_REGIME` / `TRUE_RANGE_NOT_IDENTIFIABLE` under that byte-identical N1 code; the wheel reproduces them by construction. *Disclosed limitation (not a defect):* the exact per-bar OHLC is **not** persisted in the read-only journal, and re-fetching it would require a second MT5 session against the live terminal, which the mandate forbids disturbing — so the 16-bar parity is established by byte-identity of the N1 code (completely verifiable) + behavioral reproduction, not an exact-OHLC empirical re-run.

## §5 — Data identity · PASS
**Which field binds full bar content:** `N1ReplayResult.last_closed_bar` carries the actual `Bar` (full OHLC of the evaluated bar), and `N1ReplaySnapshot.observed_bars` persists the complete raw bar history. `input_data_identity = market_event_id = symbol:timeframe:ts_close` (no OHLC); `output_fingerprint = _fp(n1_output_fp, router_output_fp)` binds the **N1 output** (the discrete axes reduction) — correct for replay parity, and **sensitive to axis-affecting OHLC changes** (demonstrated: TREND_UP→TREND_DOWN flips it). A sub-axis OHLC change leaves the output fingerprint equal but is **still bound** by `last_closed_bar` and `observed_bars` — the composite identity never silently equates different data. Timestamp change → different `input_data_identity`; unordered → refuse; bar added/deleted → different; `last_closed_bar` bound to the evaluation. Per the CEO's own rule, content **is** bound by a persisted identity, so this is not FAIL; VE's phrasing that "OHLC sensitivity is carried by `output_fingerprint`" is accurate for axis-affecting changes and completed by the composite for the rest — a purely documentary nuance.

## §6 — Replay, snapshot & causality · PASS
From the installed wheel: identical replay → deterministic; duplicate identical bar → idempotent (`PARTIAL`); duplicate **conflicting** bar → `DuplicateBarError`; future bar (`as_of < ts_close`) → `FutureBarError`; unordered → `OutOfOrderBarError`; NaN/Inf → `NonFiniteAxesInputError`; snapshot→restore → identical continuation; incompatible snapshot (different symbol) → `IncompatibleSnapshotError`; changed pin (`implementation_commit`) → `IncompatibleSnapshotError`; reset → clean initial state; **zero lookahead** (the first N results are identical whether or not later bars exist).

## §7 — Independence · PASS
In the empty venv (numpy + ve_brain + ve_n1_replay only): the ai_trader repo is absent from `sys.path`; after `import ve_n1_replay`, `MetaTrader5`/`ve_tower`/any execution/broker module are **not** in `sys.modules`; no source file has an actual `import MetaTrader5` / `import ve_tower` / broker / execution statement (the only mentions are comments/docstrings). No `order_send`, no `set_authority`, no `probability_inputs`, no legacy fallback — absent by construction. **ve_brain 0.1.3 installed from the exact pinned wheel** (`edd208ad…`), not reconstructed. ve_tower's detectors are **not** substituted — a foreign ve_tower `market_structure` triggers the fail-closed collision (§3).

## §8 — Tests & rollback · PASS
**18 tests pass** (matches VE's declared count) — coverage spans the material conditions (identity/parity/replay/snapshot/collision/independence). mypy `--strict` clean (exit 0). Clean install → import OK; uninstall → `ModuleNotFoundError`; reinstall → `0.1.0`. Wheel physical-byte verified (SHA-256 + git-stored bytes, §1). Small test count is not itself a defect — material conditions are covered.

## §9 — Live system state (read-only) · PASS
Vocabulary corrected: the global LIVE_SHADOW authority is **NEW_BRAIN** (not inactive); the N1 artefact is authority-independent and never touches it (`set_authority` absent by construction). Read-only checks: LIVE_SHADOW process alive and healthy; shadow journal now 68 records, **all `LIVE_SHADOW_NO_TRADE`, `order_send_calls=0`, none reached the broker gate**; broker DISABLED; balance/equity `1800.34` unchanged; **the AI Trader runtime (`eb97a80`) does not import `ve_n1_replay`** — the N1 artefact does not appear in the live process.

---

## AUTHORIZATION
**N1_HANDOFF_PASS · ALPHA_CANONICAL_RERUN_AUTHORIZED.** Alpha may install **exactly** `ve_n1_replay-0.1.0-py3-none-any.whl` (`372b35f9…0eb3f1`) **only in the Alpha environment** (with pinned `ve_brain 0.1.3`) and rerun the 355 hypotheses. This does **not** authorize the broker and does **not** modify LIVE_SHADOW. Any broker activation requires a separate explicit CEO mandate. **LIVE_SHADOW continues untouched with the broker DISABLED; CAND-T05 stays frozen.** Red Team modified no engine, ran no real orders, disturbed no live process, changed nothing outside `red_team/`.
