# ALPHA_MULTIPLE_TESTING_LEDGER

Cumulative search lineage for the state-path method (§15, §21).
- **Stage A univariate (this cycle):** 10 state variables x 2 sides x 2 horizons, headline label P(+100/-70) = ~40 univariate screens on DEV 2021-2023 H1. Selection = consistency across horizons+sides+monotonicity (not a single cell). Winner: `trend` (extension) — largest coherent lift both sides; secondary vol_ratio/vol_change/effic.
- No thresholds mined (continuous decile response, §6). No DISC/CONF viewed yet.
- **Prior program lineage (separate, named-pattern method):** 42 hypotheses / 19 frontiers across 2021-2023 native + historical b0/b1 SWING + b0/b1 M15 intraday + external S2/S4 -> 1 survivor COMP-CONT-L-rr2; exogenous blocked on data. Any state-method survivor discloses BOTH lineages.
- Discipline: a state candidate must beat base rate materially (§12) AND survive per-year + DISC/CONF (§14) AND neighboring-region stability (§13) before Stage-C conversion. FDR/multiplicity awareness: ~40 screens -> require lift well beyond noise (the trend signal's |lift| 0.08-0.19 and cross-consistency clear the univariate bar; stability is the real test).

- **ST-TREND-EXH validation (this cycle):** per-year x DISC/CONF x multi-horizon(5) x neighboring-thr(3) x interaction(3) x cross-pop(b0/b1) = one candidate rigorously stress-tested (not mined). Verdict KILLED (DISC+->CONF-, cross-pop~0, 2023 inverts). The promising univariate lift was a 2021-22 regime transient. No strategy built. Next: state-transition family (§8).

- **Transition screen (this cycle):** 9 causal transitions x 2 sides on 2021-2023 H1 DEV, each evaluated with per-year + DISC/CONF stability in-screen (not mined). Result: 1 STABLE = T3 up-eff-drop LONG-avoidance filter (negative, not a trade); all apparent positive edges (T9-S, T5-L, T3-S) are regime transients (DISC->CONF inversion or single-year); T8-S insufficient (N=91, 2023-only). No positive tradeable state/transition survives on 2021-2023 H1. Next family: path-history (MFE/MAE-so-far), multi-TF, session-conditioned.

- **Path-history screen (this cycle):** 10 causal path-history states x 2 sides on 2021-2023 H1 DEV + cross-pop b0/b1. Found the first within-DEV STABLE POSITIVE lift (clean_up/shallowPB_up SHORT +0.035/+0.039, all-years + DISC/CONF) -> KILLED by cross-population (b0/b1 inverts to -0.01..-0.05). META: within-period stability is insufficient; state->path is regime-conditional; cross-population is the real gate. Next: multi-TF states.

- **Multi-TF screen:** 7 HTF states (H4/D1 trend/eff/ext) -> H1 path, DEV + cross-pop. No cross-stable edge (trend-beta ~0 on b0).
- **Session screen:** 3 sessions x 2 sides, DEV + cross-pop. London-SHORT/NY-LONG stable-but-immaterial (+0.002..0.012 cross-pop).
- **STATE-PATH METHOD COMPLETE (5 families):** static / transition / path-history / multi-TF / session. No material cross-population-stable non-redundant positive edge. Central finding: price-only state->path is REGIME-CONDITIONAL. Bounded conclusion in ALPHA_STATE_PATH_BOUNDED_CONCLUSION.md. Total state-path search: ~50 state/transition definitions x 2 sides x stability battery; 1 candidate reached cross-pop (path-history) and was killed.

- **Within-regime SHORT screen (DOWN, QUIET):** 8 causal states x 2 regimes, DEV lift over same-regime base + within-regime DISC/CONF + per-year + same-regime cross-era b0/b1. Strongest = DOWN+falling-vol +0.097 (within-DEV flawless) -> FAILS same-regime cross-era (b0 -0.029). No material same-regime-stable SHORT. Finding: state->path is era-dependent even WITHIN a fixed causal regime. Next: UP/CHOP LONG + regime transitions (§27).

- **UP/CHOP LONG + regime-transition screens:** 7 LONG states x 2 regimes + 8 transitions, same-regime/same-transition cross-era gate. Only same-transition-cross-era-stable positive = QUIET->UP LONG (tiny N + REDUNDANT w/ COMP-CONT-L). No new non-redundant edge. REGIME + TRANSITION SWEEP COMPLETE.
- **Regime-conditional method total:** taxonomy (5 regimes) + baselines + within-regime states (DOWN/QUIET SHORT, UP/CHOP LONG, ~30 states) + 8 transitions. 1 stable positive = the already-frozen COMP-CONT-L edge (re-discovered). Bounded conclusion in ALPHA_REGIME_CONDITIONAL_BOUNDED_CONCLUSION.md.

- **M15 mandate Stage 1 (baselines):** built causal M15 first-passage engine (state_path_m15.py) + unconditional baselines on 2021-2023 gated M15 (N=40,649) + b0/b1 M15 (52k each). M15 L/S near-symmetric; natural excursion ~44-52p; base rates era-dependent. No states screened yet. Next: M15 univariate state info map + M15 transitions (§8 priority) with same-regime cross-era gate + event-dedup.

- **M15 univariate state map:** 16 causal M15 states x 2 sides -> P(+70/-50) 8h lift, event-deduped, per-year+DISC/CONF+cross-era. FIRST cross-era-stable signals found: all volatility-based; the directional one = high/rising-vol -> M15 SHORT bias (+0.058, stable all eras, LONG not lifted). Stable INFO; tradeability TBD (P 0.32 < breakeven). Next: expectancy/geometry characterization + M15 transitions + H1/H4 conditioning.

- **M15 high-vol-short tradeability:** fixed brackets (4) + structural ATR stops (3x3) on DEV+b0+b1, event-deduped + parent-regime split (5) + down-disp interaction. Univariate NOT tradeable cross-era (STABLE_INFO_NOT_TRADEABLE). Lead: DOWN-parent-regime high-vol-short +0.102 DEV -> cross-era check pending. No candidate frozen.

- **DOWN-parent high-vol-short cross-era (decisive):** DEV +0.112 (best10+), b0 sign-confirmed (+, best10<0), b1 CONTRADICTS (neg). REGIME_SPECIFIC_INFO_NOT_CROSS_ERA_STABLE -> not frozen. Confirms shorts are genuine-downtrend-specific (b1 uptrend-corrections fail). No candidate. Next: M15 transitions + deeper M15 states.

- **M15 transition family:** 10 causal transitions (compression->expansion, inefficient->directional, directional->collapse, high-vol->stabilization, extension->pullback) x natural side -> P(+70/-50) lift, event-deduped, per-year+DISC/CONF+cross-era. Only cross-stable = highvol->stab LONG-avoidance (filter). No positive tradeable transition. M15 state+transition maps now cover: volatility (stable info, not tradeable) + all other states/transitions (regime/era-transient or immaterial). Session-conditioned = last untested family before bounded M15 conclusion.

- **M15 session family (§13, last untested family):** 3 tests x 4 sessions x 2 sides vs SESSION base, event-deduped, cross-era. Structural findings (London/NY-open bilateral lift) = range-expansion/vol-timing (both sides), NOT directional; Off dead-zone = stable avoidance filter. ONE directional cross-era-stable candidate: NY-session high-vol->SHORT +0.070 (b0+.08/b1+.05) — registered ST-M15-NY-HIGHVOL-SHORT, pending tradeability. This is the ONLY M15 lead to pass the b1 gate directionally; all others (univariate, transitions, DOWN-parent) failed b1 or were bilateral.

- **ST-M15-NY-HIGHVOL-SHORT tradeability (decisive):** 4 fixed brackets + 9 structural ATR stops x 3 eras (DEV/b0/b1), STRESS, event-deduped. ALL net-negative; best10 -0.15..-0.38 everywhere. Cross-era-stable INFO does NOT convert to tradeable expectancy. => M15 standalone directional frontier EXHAUSTED (baselines+univariate+transitions+session all systematically completed, §27). Bounded M15 conclusion presented; CEO decision requested.

## Mandate ALPHA-XAUUSD-H4-M15-PATH-SHAPE-DISCOVERY-001 (Decision B)
- **Cycle 1 foundation (`h4_parent.py`):** froze causal H4 parent-state taxonomy (reused frozen state_regime.regime() on H4; QUIET=research-local neutral, NOT canonical RANGE) + per-H4-state M15 first-passage base rates (P(+50/-50,+70/-50,+100/-70,+100/-100) L/S + MFE/MAE + adverse-first), DEV/b0/b1, event-deduped (RAW/EffN/days/H4-episodes). NO outcome-driven state tuning. All 5 states EffN>=500 each era. Foundation only — no candidate tested yet; the base rates are the §8 comparison denominators for all subsequent path-shape lifts.

- **Family 1 (run-length/persistence, `h4m15_runlen.py`):** 5 H4 states x 9 descriptors x 2 sides x 2 targets, lift vs same-H4-state base, deduped, DISC/CONF+per-year+b0+b1. NO cross-era-stable positive tradeable lift. Only cross-stable cell = QUIET x runUp>=4 short -0.055 (negative avoidance + per-year flip). Strongest positive DOWN x persistDn short +0.059 fails b1 (era-conditional continuation). Pullback-in-trend hypothesis NOT supported. PIVOT to Family 2.

- **Family 2 (impulse->retracement geometry, `h4m15_impretr.py`):** 5 H4 states x 8 geometry descriptors x 2 sides x 2 targets, lift vs same-H4-state base, deduped, DISC/CONF+per-year+b0+b1. FIRST cross-era-stable positive candidate: DOWN x impDn8&shallow SHORT +0.055 (b0+0.04/b1+0.03, survives incl b1), neighboring-window + mirror-coherent. Registered ST-H4DN-M15DNIMP-SHALLOW-SHORT (pending characterization). Other cells fail (QUIET impUp16 short reverses cross-era). NEXT = characterize the candidate (§8/§14/§15/§16/§17).

- **ST-H4DN-M15DNIMP-SHALLOW-SHORT characterization (`h4m15_dnimp_char.py`):** structural-stop (recent swing high) short, net STRESS cross-era. Tradeable in b0 (+0.089..+0.200) but net-negative DEV (breakeven, losing 2022) + b1 (-0.02..-0.05). Cross-era-stable relative info does NOT convert to cross-era-stable expectancy (base rate + move size scale with era vol). Fails robust-survivor bar (§22); not frozen; not vol-sub-cut (would re-tune frozen taxonomy). Independence vs COMP-CONT-L 4/72 shared-days, opposite dir (non-redundant, moot). PIVOT to Family 3.

- **Family 3 (recovery/asymmetry/vol-exp-calm, `h4m15_recovery.py`):** 5 H4 states x 6 descriptors x 2 sides x 2 targets, lift vs same-H4-state base, deduped, DISC/CONF+per-year+b0+b1. NO cross-era-stable candidate. Strongest UP x dipRecovUp LONG +0.059 (DEV all-years+DISC/CONF, coherent mirror) fails b1 (-0.01). Reinforces: b1 low-vol is the binding cross-era constraint (both continuation & reversal path-edges fail to extend to b1). PIVOT to Family 4.

- **Family 4 (swing-sequence + wick, `h4m15_swingwick.py`):** 5 H4 states x 6 descriptors x 2 sides x 2 targets. 2 CROSS_STABLE flags but both non-directional: CHOP x HH_LL short +0.049 = BILATERAL range-expansion (long also +0.041; CHOP no directional thesis) = vol-timing; UP x uwDom short -0.061 = negative avoidance. HH_LL expansion lifts both sides everywhere; LH_HL contraction lowers both everywhere (vol-structure, cross-era-stable, non-directional). Directional structure (LH_LL short) era-conditional (b1 flat). NO new directional candidate. Convergence w/ standalone-M15. PIVOT to Family 5.

- **Family 5 (fresh-extreme + curvature, `h4m15_curvature.py`):** 5 H4 states x 6 descriptors x 2 sides x 2 targets. Momentum-extreme (freshHigh/Low) fail cross-era (b1~0). ONE cross-era-stable candidate: DOWN x decelDn short +0.050/+0.059 (b0+0.06/b1+0.04) = SAME DOWN-H4-short-continuation family as F2. Registered ST-H4DN-M15-DECELDN-SHORT, pending characterization + redundancy-vs-F2 check. NEXT = characterize then bounded interim conclusion.

- **ST-H4DN-M15-DECELDN-SHORT characterization (`h4m15_deceldn_char.py`):** structural-stop net STRESS cross-era + §17 redundancy. Tradeable b0 only (DEV -0.010/2022 -0.35; b1 marginal) = era-conditional (same as F2). §17: 100% DEV day-overlap w/ F2 (62/62), 36 shared bars -> REDUNDANT_EXISTING_ALPHA. Not frozen (2 grounds). DOWN-H4-short family = era-conditional + triggers mutually redundant. NEXT = §21 transition-map + interactions, then bounded conclusion.

- **§21 completion (transitions + interactions, `h4m15_trans_interact.py`):** squeeze->release (UP->L +0.107 DEV but thin/b1-flat; DOWN->S b1-neg), pullback->resume (zero-info), 2 interactions. Only cross-stable = LH_LL&decel@DOWN->S +0.055 = 3rd trigger of the DOWN-H4-short mechanism (not independent, §18). NO new candidate. §21 budget complete -> bounded interim conclusion.

## Mandate ALPHA-XAUUSD-DXY-CAUSAL-INCREMENTAL-INFORMATION-001 (Decision A — exogenous DXY axis)
- **Cycle 1 foundation (`dxy_data.py`):** causal loader/aligner for ratified ICE DXY H1 <-> XAUUSD H1 (_from_M15_v2). Coverage verified == ratified report (97.4/97.8/99.9% same-hour), causal-leak assertion passes. Predeclared DXY feature set (8 features) + lag set {0,1,2,4}H frozen. Sanity: corr(past DXY 4h ret, XAUUSD fwd 24h ret) ~0 all eras -> DXY<->gold inverse is CONTEMPORANEOUS not predictive. Foundation only, no information map yet.

- **Stage A univariate (`dxy_infomap.py`):** 6 DXY states (impulse/accel/efficiency x up/dn) x directed side x 5 labels x lag {0,1,2,4} x 3 eras, lift vs XAUUSD era base, deduped. NO cross-era-stable univariate DXY directional info. Strongest coherent = persistent DXY efficiency -> inverse XAUUSD path, +0.02..+0.03 in b0/b1 but REVERSES -0.02..-0.04 in 2021-2023 (regime inversion, inflation/safe-haven era). Lag decays from lag0. Lifts <=0.04. NEXT = X3 divergence + §7 incremental test + interactions.

- **X3 divergence + §7 incremental (`dxy_divergence_incremental.py`):** X3 (gold-not-reacting divergence, DISC-thresholded) flips sign b0 vs b1 -> not stable. §7 incremental (persistent DXY dir over XAUUSD parent regime): b0/b1 small POSITIVE increment (+0.02..+0.06, NON-redundant) but INVERTS in y2123. DXY incremental info is REAL in 2011-2018 but regime-conditional (inverts 2021-2023) -> fails material+stable. NEXT = DXY transitions then bounded conclusion.

- **DXY transitions (`dxy_transitions.py`):** 4 DXY A->B transitions (impulse-exhaustion, reversal) x directed side x 3 eras. NO cross-era-stable transition (usdUpExhaust->L +0.037/+0.044 b1/y2123 but ~0 b0; usdRevDn->L flips b0 vs b1). §20 order complete. -> bounded DXY conclusion.

## Mandate ALPHA-XAUUSD-HIERARCHICAL-MODE-STRUCTURAL-EVENT-DISCOVERY-001 (architecture reset — mode->event->specialist)
- **Cycle 1 (`market_mode.py`):** froze MARKET_OPERATING_MODE_V1 (price-only causal H4; 6 modes via two-scale primary backbone P=30 vs immediate I=6; separates PRIMARY_BEAR from BULL_CORRECTION per §4). Population stable across b0/b1/2021/2022/2023, all modes sufficient N/episodes; transition matrix economically coherent (sticky primary, corrections return to primary). NO P&L in taxonomy. Foundation only — no events yet.

- **Liquidity-event decomposition first cut (`liquidity_event.py`):** 6 modes x 2 sides (sell->L/buy->S), decomposition base->+evt->+rcl->+disp, P(+70/-50) 8h, deduped, per era. Finding: DISPLACEMENT is the informative component (validates §13/§14); raw sweep +evt ~0. Two mode-conditional cross-era-consistent (b0+b1) disp-driven leads: BEAR_CORRECTION sell-sweep-disp->L (+0.144/+0.045), BULL_CORRECTION buy-sweep-disp->S (+0.064/+0.046). Counter-trend cells fail cross-era. Small disp-N (31-44). Leads not survivors. NEXT = deepen 2 leads (more eras + +100/-70 + winner-vs-loser + tradeability).

- **Liquidity leads deepened (`liquidity_deepen.py`):** 2 single-bar leads (BEAR_CORR sweep->L, BULL_CORR sweep->S) FAIL: rare (~2 eff/mo), structural stop too tight (16-30p, single-bar formulation), not cross-era-robust within mode (A +b0/-b1, B -b0/marginal-b1), winner-loser non-monotonic. First-cut disp lift was small-N noise. Root = single-bar event formulation. NEXT = one predeclared multi-bar liquidity-SEQUENCE reformulation (sweep+reclaim then displacement over K bars).
