# ALPHA_REGIME_STRATEGY_REGISTRY

Regime-specific strategy candidates (§19, §31). A candidate binds REGIME_CONTRACT + SIGNAL_CONTRACT + ENTRY + SL + TARGET/HORIZON as one identity. Multiple regime-specific strategies authorized (§26).

| STRATEGY_ID | regime contract | signal | side | status |
|---|---|---|---|---|
| QUIET->UP-LONG (obs) | QUIET->UP onset (compression release into uptrend) | forward long | L | **SAME-TRANSITION-CROSS-ERA-STABLE (+0.099 DEV/+0.048 b0b1) but NOT promoted** — tiny N (40/72, §16) AND REDUNDANT_EXISTING_ALPHA (= COMP-CONT-L compression->uptrend thesis). Independently confirms COMP-CONT-L as a genuine regime-transition edge. |
| (no NEW non-redundant regime-specific survivor) | — | — | — | regimes UP/DOWN/QUIET/CHOP + 8 transitions screened; only stable positive = the already-frozen COMP-CONT-L edge |

**Frozen reference objects (comparison only, §21-24):** S5 (validated benchmark); COMP-CONT-L-rr2 (FROZEN_PENDING_INDEPENDENT_VALIDATION; may later be interpreted as UP-regime-specific LONG, do not alter); **H4-bo-raw-S (INDEPENDENT_VALIDATION_BLOCKED — non-causal legacy D1 filter confirmed; reference/overlap only, NOT a validated portfolio strategy).**
