"""Sealed Q4 1-378 dev/test fixture -- see `materialize_sealed_fixture.py`. `data/` holds the
already-materialized output this package's tests actually read; nothing at import time here opens
the full, unsealed source file."""
