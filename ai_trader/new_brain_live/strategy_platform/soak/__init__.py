"""Section 36 soak harness -- see `harness.py`'s own module docstring for exactly what this does and does
not exercise (a component/integration soak of the real `strategy_platform` pipeline, never a live-MT5
soak, never touching `AITraderLiveShadow`)."""
