"""LIVE_SHADOW -- the continuous, unattended consumer of `new_brain_bridge` against real closed MT5 bars
and the real, installed `ve_tower` chain. CEO authorization, 2026-08-17 (`AUTORIZEZ PORNIREA LIVE_SHADOW`,
RT-MANDATE2-0003, Red Team commit `b05cbcb`): shadow decision computation and telemetry only -- the
broker barrier (`BrokerOrderSubmissionGate`, default `enabled=False`) is never touched by this package,
and `order_send` is never imported here."""
